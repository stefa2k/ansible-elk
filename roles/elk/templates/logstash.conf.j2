input {
        tcp {
                port => 5000
        }
        beats {
                port => 5044
        }
}

{{logstash_custom_filters}}

output {
        elasticsearch {
                hosts => [{% for host in ansible_play_hosts %}"{{host}}"{% if not loop.last %}, {% endif %}{% endfor %}]
        }
}