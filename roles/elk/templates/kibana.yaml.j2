server.name: kibana
server.host: "0"
elasticsearch.hosts: '[ {% for host in ansible_play_hosts %}"http://{{hostvars[host].elk_listen_ip}}:9200"{% if not loop.last %}, {% endif %}{% endfor %} ]'
xpack.monitoring.ui.container.elasticsearch.enabled: false
